---
interface Props {
    icon: any; // Recibimos el componente .astro (ícono inline)
    title: string;
    class?: string; 

}

const { icon: Icon, title, class: customClass  } = Astro.props;
---

<div class={`bg-white shadow-md rounded-lg p-6 text-left space-y-4 ${customClass ?? ''}`}>
    <figure class="w-16">
        <Icon  />
    </figure>
    <h2 class="text-2xl font-semibold">{title}</h2>
     
    <slot/>
   
</div>

